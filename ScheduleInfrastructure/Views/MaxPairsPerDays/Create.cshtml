@model ScheduleDomain.Model.MaxPairsPerDay

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Max pairs per day</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="MaxPairs" class="control-label"></label>
                <input asp-for="MaxPairs" class="form-control" />
                <span asp-validation-for="MaxPairs" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" style="margin-top: 5px" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index" class="btn btn-secondary" style="margin-top: 5px">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
